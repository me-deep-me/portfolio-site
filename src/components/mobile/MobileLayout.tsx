'use client';

/**
 * Mobile layout is no longer used — page.tsx is now fully responsive.
 * Stub kept for backward compatibility with any stale imports.
 */
export function MobileLayout() {
  return null;
}
